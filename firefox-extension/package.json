{
  "name": "linky-archiver-extension",
  "private": true,
  "version": "1.0.0",
  "description": "Archive web pages to content.oglimmer.com via Linky",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "package": "npm run build && web-ext build --source-dir . --artifacts-dir web-ext-artifacts --ignore-files src node_modules esbuild.config.mjs package-lock.json"
  },
  "dependencies": {
    "single-file-core": "^1.5.84"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "web-ext": "^8.4.0"
  }
}
